{
  "name": "aln-functions",
  "description": "ALN Platform — Cloud Functions",
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
